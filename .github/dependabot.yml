# Actions are pinned to commit SHAs, so a moved tag runs nothing new; this is
# how they are updated, one reviewed pull request a week.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
    commit-message:
      prefix: ci
